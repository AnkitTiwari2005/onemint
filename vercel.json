{
  "_comment": "This file is intentionally kept but is INERT on the current hosting platform (Hostinger).",
  "_comment2": "Vercel Cron config below does nothing on Hostinger — cron jobs must be set up in hPanel > Cron Jobs.",
  "_comment3": "Hostinger cron setup: Authorization: Bearer $CRON_SECRET header on the URLs below.",
  "_hostinger_monday":  "30 8 * * 1  curl -s -H 'Authorization: Bearer $CRON_SECRET' https://www.onemint.in/api/cron/newsletter?series=monday",
  "_hostinger_sunday":  "30 9 * * 0  curl -s -H 'Authorization: Bearer $CRON_SECRET' https://www.onemint.in/api/cron/newsletter?series=sunday",
  "crons": [
    {
      "path": "/api/cron/newsletter?series=monday",
      "schedule": "30 2 * * 1"
    },
    {
      "path": "/api/cron/newsletter?series=sunday",
      "schedule": "30 3 * * 0"
    }
  ]
}
